import { shallowMount } from '@vue/test-utils';
import Banner from '@/components/utils/banner/Banner.vue';
import SearchBar from '@/components/utils/search-bar/SearchBar.vue';

describe('Banner.vue', () => {
  it('renders props.title when passed', () => {
  });
});
